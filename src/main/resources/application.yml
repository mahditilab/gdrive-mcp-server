spring:
  main:
    banner-mode: "off"
  ai:
    mcp:
      server:
        name: gdrive-mcp-server
        version: 0.0.1
        stdio: true   # STDIO transport — works with Claude Desktop, Copilot, Cursor, etc.
        type: SYNC

google:
  drive:
    tokens-path: ${GDRIVE_TOKENS_PATH}
    application-name: gdrive-mcp-server
